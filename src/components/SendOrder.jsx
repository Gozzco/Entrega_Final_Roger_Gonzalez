import { Button, Link } from '@chakra-ui/react'
import React from 'react'

const Form = () => {
  return (
    <Button> <Link to={'/contact'}>Finzalizar la compra</Link> </Button>
  )
}

export default Form